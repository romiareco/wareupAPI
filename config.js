const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "wareupadmin",
      password: "Pass123*",
      database: "wareup",
    },
    listPerPage: 10,
  };
  module.exports = config;